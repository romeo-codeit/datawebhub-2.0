// import { config } from 'dotenv';
// import { resolve } from 'path';

// // Load environment variables from .env file in the project root
// // process.cwd() will be the 'server' directory when 'npm run dev' is executed from root
// // so '../.env' correctly points to the .env file in the project root.
// config({ path: resolve(process.cwd(), '../.env') });

// // Optional: Add checks here to ensure variables are loaded
// // if (!process.env.APPWRITE_ENDPOINT) {
// //   console.error('Error: APPWRITE_ENDPOINT is not set in .env file!');
// // }
// // if (!process.env.APPWRITE_PROJECT_ID) {
// //   console.error('Error: APPWRITE_PROJECT_ID is not set in .env file!');
// // }
// // if (!process.env.APPWRITE_API_KEY) {
// //   console.error('Error: APPWRITE_API_KEY is not set in .env file!');
// // }
